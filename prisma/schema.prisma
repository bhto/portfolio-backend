generator client {
    provider        = "prisma-client-js"
}

datasource db {
    provider = "sqlite" 
}

model Visit {
    id             String   @id @default(cuid())
    clientId       String
    createdAt      DateTime @default(now())
}
